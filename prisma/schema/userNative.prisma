model UserNative {
    id  Int @id @default(autoincrement())
    email String @unique
    username String
    password String
    isVerified       Boolean  @default(false)
    verificationCode String?
}